[pytest]
# 테스트가 app 패키지를 찾을 수 있게 지금 폴더를 경로에 넣습니다.
# 이게 없으면 `pytest` 만 쳤을 때 "No module named 'app'" 이 납니다.
# (`python -m pytest` 는 우연히 되기 때문에 오래 모르고 지낼 수 있습니다.
#  2026-09-20 에 CI 를 켜자마자 이게 잡혔습니다.)
pythonpath = .
testpaths = tests

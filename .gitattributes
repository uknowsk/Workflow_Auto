# 회사 PC(Windows)에서 쓰는 PowerShell 스크립트는 항상 CRLF 로 내려받게 고정합니다.
# git 설정(core.autocrlf)이 사람마다 달라도 이 줄이 있으면 결과가 같습니다.
*.ps1 text eol=crlf

# 리눅스/도커에서 도는 셸 스크립트는 반대로 항상 LF 여야 합니다.
# (CRLF 로 내려오면 "bad interpreter: No such file or directory" 로 안 뜹니다.)
*.sh text eol=lf
